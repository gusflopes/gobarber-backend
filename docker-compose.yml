version: '3'

services:
<<<<<<< HEAD
  gobarber:
    build: .
    environment:
      NODE_ENV: ${NODE_ENV}
    volumes:
      - .:home/node/api
      - /home/node/api/node_modules
    ports:
      - '${APP_PORT}:3333'

volumes:
  gobarber:

networks:
  backend:
=======
  postgres:
    image: postgres:12
    container_name: postgres
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASS}
      POSTGRES_DB: ${DB_NAME}
    ports:
      - '5432:5432'
    volumes:
      - pg-data:/var/lib/postgresql/data
    networks:
      - gobarber-network

  mongo:
    image: mongo
    container_name: mongo
    ports:
      - '27017:27017'
    volumes:
      - mongo-data:/data/db
    networks:
      - gobarber-network

  redis:
    image: redis:alpine
    container_name: redis
    ports:
      - '6379:6379'
    volumes:
      - redis-data:/data
    networks:
      - gobarber-network

volumes:
  pg-data:
  mongo-data:
  redis-data:

networks:
  gobarber-network:
>>>>>>> 97a133813202ad7ca328958cbe870f555d876c21
    driver: bridge
